[package]
name = "oc-guardian-fedimint"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "fedimintd binary lifecycle management for the OC guardian kit (download, verify, run, attest)."

[dependencies]
anyhow.workspace = true
tracing.workspace = true
serde.workspace = true
serde_json.workspace = true
hex.workspace = true
sha2.workspace = true
ureq.workspace = true
oc-guardian-core = { workspace = true }

[dev-dependencies]
# Tests only: produce a real deterministic Ed25519 signature for the
# cross-language settlement parity vector (verified by the portal's @noble
# verifier). The runtime signing path goes through the core Signer.
ed25519-dalek.workspace = true
